{% extends 'base.html.twig' %}

{% block title %}Nurses{% endblock %}

{% block body %}
    <h1>Nurses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nurse.id }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ nurse.user }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ nurse.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nurses_index') }}">back to list</a>

    <a href="{{ path('app_nurses_edit', {'id': nurse.id}) }}">edit</a>

    {{ include('nurses/_delete_form.html.twig') }}
{% endblock %}
